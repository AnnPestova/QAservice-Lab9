<!-- header -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
        <div class="container">
            <a class="navbar-brand" href="/">QA</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                </ul>
                <form class="d-flex">
                    {% if app.user %}
                        <a href="/api" class="btn btn-outline-success d-lg-inline-block my-2 my-md-0 ms-md-3">api</a>
                        <button id="copyApiToken"
                                value="{{ app.user.apiToken }}"
                                class="btn btn-outline-success d-lg-inline-block my-2 my-md-0 ms-md-3">Копировать API-ключ
                        </button>
                        <a href="/profile" class="btn btn-outline-success d-lg-inline-block my-2 my-md-0 ms-md-3">{{ app.user.getName() }}</a>
                        <a href="/add/question" class="btn btn-outline-success d-lg-inline-block my-2 my-md-0 ms-md-3">Оставить вопрос</a>
                        <a href="/logout" class="btn btn-outline-danger d-lg-inline-block my-2 my-md-0 ms-md-3">Выход</a>
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="/admin" class="btn btn-outline-danger d-lg-inline-block my-2 my-md-0 ms-md-3">Админка</a>
                        {% endif %}
                    {% else %}
                        <a href="/login" class="btn btn-outline-success d-lg-inline-block my-2 my-md-0 ms-md-3">Авторизация</a>
                        <a href="/register" class="btn btn-outline-primary d-lg-inline-block my-2 my-md-0 ms-md-3">Регистрация</a>
                    {% endif %}
                </form>
            </div>
        </div>
    </nav>
